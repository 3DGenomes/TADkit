(function () {
	'use strict';
	/**
	 * @ngdoc overview
	 * @name generic
	 * @module generic
	 * @description
	 * Generic Module
	 * Contains generic scripts which are not available on Bower
	 * These are not app-specific but essential to the App.
	 *
	 * @example
	 * `angular.module('myModule',['generic']);`
	 *
	 */
	angular
		.module('generic', []);
}());